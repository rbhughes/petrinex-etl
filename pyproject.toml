[project]
name = "petrinex-etl"
version = "0.1.0"
description = "Fetch and normalize Petrinex public volumetric data (Alberta, Saskatchewan)"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.0",
    "requests>=2.32",
]

[project.scripts]
petrinex = "petrinex_etl.cli:main"

[project.optional-dependencies]
dev = ["pytest>=8"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
